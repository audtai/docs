/**
 * AUDT-facing feature switches.
 *
 * Keep disabled integrations wired in the codebase so they can be restored
 * deliberately without rebuilding their underlying configuration.
 */
export const DOCSEARCH_ENABLED = false;
